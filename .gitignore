# Local settings (permissions, machine-specific config)
.claude/settings.local.json

# Credentials (NEVER commit)
*.json
!package.json
!tsconfig.json
!graphify-out/**/*.json

# Graphify machine-specific interpreter path (do not commit)
graphify-out/.graphify_python

# Personal medical PDFs (NEVER commit — contain personnummer + medical data)
*.pdf
Kallelse*
Provsvar*

# Personal medical images + generated HTML reports (NEVER commit — private health data)
*.jpeg
*.jpg
html-output/

# OS files
.DS_Store
Thumbs.db
